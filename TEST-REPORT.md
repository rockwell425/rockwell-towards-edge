# RockwellTowards v0.4.3 候选版测试报告

## 修复目标

1. ChatGPT 对话档案能够发现用户图片与 AI 生成图片，而不是仅依赖当前 DOM。
2. 能处理图片位于隐藏 `image_gen/tool` 消息中的情况。
3. 能将 `file-service://`、`sediment://` 等资源指针解析为可下载图片。
4. 长对话消息数不一致时不再错误显示“完整”。

## 自动测试

已通过：

- 扩展目录全部 JavaScript 文件 `node --check`。
- `manifest.json` 解析与版本检查。
- ChatGPT 活动分支文本、代码块、模型名称和 DOM 合并测试。
- 5000 条合成长对话解析与收集测试。
- 用户图片档案指针解析测试。
- AI 图片位于 `tool/image_gen` 隐藏消息时的合并测试。
- `sediment://file-ai-1` 转换为 ChatGPT 文件下载请求测试。
- 后台携带登录令牌读取图片并转换为 Data URL 的模拟测试。

合成媒体测试结果：

- 可见消息：2 条。
- 用户图片：1 张。
- AI 图片：2 张。
- 隐藏工具媒体正确合并到可见 AI 回复。

## 本地静态检查

- Manifest V3 后台服务脚本语法通过。
- 图片下载限制为 40 MiB。
- ChatGPT JSON 读取仅允许 `chatgpt.com` 与 `chat.openai.com`。
- 图片解析失败会保留资源描述和错误原因，不会把失败图片计作已嵌入。

## 尚需真实 Edge 登录环境验证

- 当前线上 ChatGPT 对话档案中图片字段的实际结构。
- 不同账号、地区与旧对话使用的文件资源端点差异。
- 真实 `oaiusercontent.com` 签名链接和 `sediment://` 资源是否都能下载。
- 包含大量生成图的长对话在“标准图”和“高清图”模式下的内存占用。
- 124/123 这类真实对话在结束刷新后是否变为严格一致。

因此该包仍标记为候选版。真实测试时应检查弹窗中的：

- `图片 X（嵌入 Y / 失败 Z）`
- `完整性 实际数/预期数`

只有图片发现数大于 0 且嵌入数符合预期，才表示图片链路真正成功。
