# Changelog

## v0.4.3 — ChatGPT 图片档案与完整性修复

### AI 生成图片与用户图片

- 不再只依赖当前页面中的 `<img>` 节点。
- 解析 ChatGPT 对话档案中的 `image_asset_pointer`、`file-service://`、`sediment://`、图片 URL、附件元数据和工具结果媒体。
- 支持从 `tool/image_gen` 一类隐藏工具消息中提取图片，并合并到对应的可见 AI 回复中。
- 支持只有图片、没有正文的 AI 回复和用户消息。
- 对图片资源 ID 尝试 ChatGPT 文件下载接口，再回退到签名图片 URL。
- 图片结果区显示：发现数量、真正嵌入数量和失败数量。
- 用户发送图片与 AI 生成/回复图片仍可分别开关。

### 长对话完整性

- 捕获开始时读取活动分支，滚动补充页面格式和媒体，结束前再次刷新活动分支。
- 修复最新一条消息尚未写入首次档案请求时出现的 `124/123` 一类错误。
- 只有“实际消息数等于预期消息数且无缺失”时才标记为完整。
- 额外页面消息会单独记录，不再在数量不一致时声称完整。

### 模型名称

- 保留具体模型识别与手动覆盖功能。
- 支持 `GPT-5.6 Thinking`、`GPT-5.4 Thinking`、`GPT-5.6 Sol` 等名称汇总显示。

### 兼容

- 版本升级为 0.4.3。
- 保留 v0.4.x 的最近记录、批量任务和界面设置。
